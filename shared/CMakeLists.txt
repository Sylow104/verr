FILE(GLOB_RECURSE SRC_C src/*.c)
FILE(GLOB_RECURSE SRC_CPP src/*.cpp)
add_library(shared ${SRC_C} ${SRC_CPP})
target_include_directories(shared PUBLIC include)
target_link_libraries(shared PUBLIC gsl)
target_link_libraries(shared PUBLIC m)
target_link_libraries(shared PUBLIC cblas)